<Window x:Class="MWPFProject_Timer.SyncSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="同步设置"
        Width="520"
        Height="250"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="SettingsButton" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="MinWidth" Value="78"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Foreground" Value="#F5F1EB"/>
            <Setter Property="Background" Value="#2E363E"/>
            <Setter Property="BorderBrush" Value="#4A5662"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#FF7A1A"/>
                                <Setter Property="Background" Value="#3B4650"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#161B20"/>
            <Setter Property="Foreground" Value="#F5F1EB"/>
            <Setter Property="CaretBrush" Value="#FF7A1A"/>
            <Setter Property="BorderBrush" Value="#3D4852"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Border x:Name="RootBorder"
            Width="520"
            Height="250"
            Background="#20262C"
            BorderBrush="#596571"
            BorderThickness="1"
            CornerRadius="8">
        <Grid Margin="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="同步设置"
                           Foreground="#F5F1EB"
                           FontSize="20"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           MouseLeftButtonDown="HeaderPanel_MouseLeftButtonDown"/>
                <Button HorizontalAlignment="Right"
                        Width="30"
                        MinWidth="30"
                        Padding="0"
                        ToolTip="关闭"
                        Style="{StaticResource SettingsButton}"
                        Click="CancelButton_Click"
                        Content="×"/>
            </Grid>

            <TextBlock Grid.Row="1"
                       Text="电脑简称（1–2 个字）"
                       Foreground="#C8D0D8"
                       FontSize="12"
                       Margin="0,0,0,7"/>
            <TextBox Grid.Row="2"
                     x:Name="DeviceNameTextBox"
                     Height="34"
                     VerticalContentAlignment="Center"
                     Style="{StaticResource SettingsTextBox}"/>

            <TextBlock Grid.Row="3"
                       x:Name="ValidationTextBlock"
                       Text="配置保存在本机 sync_config.json，保存后立即生效。"
                       Foreground="#8FA0AF"
                       FontSize="11"
                       Margin="0,12,0,14"
                       TextWrapping="Wrap"/>

            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom">
                <Button Content="取消"
                        Margin="0,0,10,0"
                        Style="{StaticResource SettingsButton}"
                        Click="CancelButton_Click"/>
                <Button Content="保存"
                        Background="#B14D0B"
                        BorderBrush="#FF8A32"
                        Style="{StaticResource SettingsButton}"
                        Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
